namespace: us-map

replicaCount: 1

image:
  repository: registry.k8s.blacktoaster.com/imperialism-frontend
  pullPolicy: Always
  tag: "83394bd"

service:
  type: ClusterIP
  port: 80

ingress:
  enabled: true
  className: "nginx"
  annotations:
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/backend-protocol: "HTTP"
    external-dns.alpha.kubernetes.io/target: "pi5.k8s.blacktoaster.com"
  host: imperialism.k8s.blacktoaster.com
  tls:
    enabled: true
    secretName: us-map-frontend-tls

certificate:
  enabled: true
  issuerName: vault-issuer
  issuerKind: ClusterIssuer

resources:
  limits:
    cpu: 100m
    memory: 128Mi
  requests:
    cpu: 50m
    memory: 64Mi
