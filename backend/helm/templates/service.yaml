apiVersion: v1
kind: Service
metadata:
  name: {{ include "us-map-backend.fullname" . }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "us-map-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "us-map-backend.selectorLabels" . | nindent 4 }}
